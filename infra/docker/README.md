# docker

Dockerfiles for all three apps (iittnif-web, dronagiri-web, cms) plus a
docker-compose.yml for local/staging parity. Built from day one so the
eventual move from Vercel to IIT Tirupati's own servers is a redeploy,
not a rebuild.
