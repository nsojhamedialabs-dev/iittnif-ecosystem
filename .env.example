# Copy to .env.local in each app that needs it

# --- iittnif-web / dronagiri-web ---
NEXT_PUBLIC_CMS_API_URL=http://localhost:3001

# --- cms (Payload) ---
PAYLOAD_SECRET=replace-with-a-long-random-string
DATABASE_URI=postgresql://user:password@localhost:5432/iittnif
